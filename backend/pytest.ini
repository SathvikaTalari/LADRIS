[pytest]
testpaths = tests
python_files = test_*.py
asyncio_default_fixture_loop_scope = function
# The workspace can be shared by different Windows identities (Codex, the
# interactive user, and Docker). Disable pytest's disposable disk cache so a
# cache created by one identity cannot break test discovery for another.
addopts = -p no:cacheprovider
